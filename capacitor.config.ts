import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'WorkFitt_Frontend',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
